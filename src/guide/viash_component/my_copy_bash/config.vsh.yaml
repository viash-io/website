functionality:
  name: my_copy_bash
  namespace: guide/viash_component
  description: A minimal example component.
  arguments:
    - type: file
      name: --input
      example: file.txt
      required: true
    - type: file
      name: --output
      direction: output
      example: output.txt
      required: true
  resources:
    - type: bash_script
      path: script.sh
  # test disabled for now
  # test_resources:
  #   - type: bash_script
  #     path: test.sh
platforms:
  - type: docker
    image: "bash:4.0"
  - type: native
  - type: nextflow