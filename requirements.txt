# Host Python packages required by _src/render_pages.sh, which runs the
# _src/automation/* components (engine: native) to regenerate the reference
# pages and version blog posts. Everything else those scripts import is stdlib.
Jinja2==3.1.6
PyYAML==6.0.2
